# Copy to .dev.vars for local wrangler dev (not committed)
# Firebase: see shared/firebase.config.ts — no vars required for auth.

# R2 presigned uploads (optional for local direct upload)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
